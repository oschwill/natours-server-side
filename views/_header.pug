header.header
  nav.nav.nav--tours
    a.nav__el(href='/') All tours        
    //- Nur .Klassenname erstellt automatisch ein div Element
  .header__logo 
    img(src='/img/logo-white.png' alt='Natours logo')
  nav.nav.nav--user
    //- greifen auf unsere res.locals.user aus dem authController in der isLoggedIn Funktion zu. Wenn user(also cookie) existiert dann geben das aus
    if user
      a.nav__el.nav__el--logout Log out
      a.nav__el(href='/me')
        img.nav__user-img(src=`/img/users/${user.photo}` alt=`Photo of ${user.name}`)
        span= user.name.split(' ')[0]
    //- wenn kein cookie, dann nicht eingeloggt dann gebe diesen header aus!
    else 
      a.nav__el(href='/login') Log in
      a.nav__el.nav__el--cta(href='/signup') Sign up